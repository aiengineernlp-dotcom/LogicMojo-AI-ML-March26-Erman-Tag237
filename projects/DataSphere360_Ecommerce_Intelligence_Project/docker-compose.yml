services:
  postgres_ml:
    image: postgres:16
    environment:
      POSTGRES_DB: datasphere360_customer_ecommerce
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5556:5432"
    volumes:
      - postgres_ml_data:/var/lib/postgresql/data

volumes:
  postgres_ml_data:




